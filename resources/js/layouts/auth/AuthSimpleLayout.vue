<script setup lang="ts">
import AppLogoIcon from '@/components/AppLogoIcon.vue';
import { Link } from '@inertiajs/vue3';

defineProps<{
    title?: string;
    description?: string;
}>();
</script>

<template>
    <div class="flex min-h-svh flex-col items-center justify-center gap-6 bg-background p-6 md:p-10">
        <div class="w-full max-w-sm">
            <div class="flex flex-col gap-8">
                <div class="flex flex-col items-center gap-4">
                    <Link :href="route('login')" class="flex flex-col items-center gap-2 font-medium">
                        <div class="mb-2 flex h-48 w-48 items-center justify-center rounded-2xl overflow-hidden bg-zinc-950 border border-zinc-800 shadow-md hover:scale-105 transition-transform duration-300">
                            <img src="/login-logo.png" class="h-48 w-48 object-cover" alt="Águilas Gym Logo" />
                        </div>
                        <span class="sr-only">{{ title }}</span>
                    </Link>
                    <div class="space-y-2 text-center">
                        <h1 class="text-xl font-medium">{{ title }}</h1>
                        <p class="text-center text-sm text-muted-foreground">{{ description }}</p>
                    </div>
                </div>
                <slot />
            </div>
        </div>
    </div>
</template>
